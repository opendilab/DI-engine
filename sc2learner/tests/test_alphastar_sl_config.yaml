data:
    train:
        dataset_type: fake
        dataloader_type: iter  # [epoch, iter]
        batch_size: 3  # shrink from 6
        trajectory_len: 5  # shrink from 48
        trajectory_type: sequential  # [epoch: [slide_window], iter: [sequential]]
        slide_window_step: 1
        use_ceph: False
        use_available_action_transform: True
    eval:
        dataset_type: fake
        use_ceph: False
        use_available_action_transform: True
logger:
    print_freq: 1
    save_freq: 200
    eval_freq: 200