league:
    # ---player----
    player_category: ['zerg', 'terran', 'protoss']  # placeholder
    active_players: 
        main_player: 1
        main_exploiter: 1
        league_exploiter: 2
    main_player:
        branch_probs:
            pfsp: 0.5
            sp: 0.35
            verification: 0.15
        strong_win_rate: 0.7
        one_phase_step: 2e3
    main_exploiter:
        branch_probs:
            main_players: 1.0
        strong_win_rate: 0.7
        one_phase_step: 2e3
        min_valid_win_rate: 0.2
    league_exploiter:
        branch_probs:
            pfsp: 1.0
        strong_win_rate: 0.7
        one_phase_step: 2e3
    use_pretrain_init_historical: True
    pretrain_checkpoint_path:  # placeholder
        zerg: 'pretrain_checkpoint_zerg.pth'
        terran: 'pretrain_checkpoint_terran.pth'
        protoss: 'pretrain_checkpoint_protoss.pth'
    # ---payoff---
    payoff:
        type: 'battle'  # ['solo', 'battle']
        decay: 0.99
        min_win_rate_games: 8
    # ---runner---
    max_active_player_task: 3
    #per_match_games: 8
    time_interval: 1
model:
    placeholder:  
